n; x; i = 0; count = 0;

START

PRINT ("How many numbers you want to read? ");
READ (n);

WHILE (i < n) {
	PRINT ("Insert the next number: ");
	READ (x);
	
	IF (x % 2 == 0) {
		count = count + 1;
	}

	i = i + 1;
}

PRINT (count);

END
