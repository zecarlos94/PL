i = 0; n; x; sum = 0;

START

PRINT ("How many numbers you want to read? ");
READ (n);

WHILE (i < n) {
	PRINT ("Insert next number: ");
	READ (x);

	sum = sum + x;

	i = i + 1;
}

PRINT ("Resultado: ");
PRINT (sum);

END
